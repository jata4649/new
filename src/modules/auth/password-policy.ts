/**
 * パスワードの要件（定数のみ）。
 *
 * ハッシュ化の実装（password.ts）はネイティブモジュール @node-rs/argon2 に依存し、
 * クライアントバンドルへ含められない。
 * 一方で入力欄のヒント表示などクライアント側でも長さの要件は必要になるため、
 * **依存を持たない定数だけをこのファイルへ分離している**。
 *
 * 長さを主軸にし、記号必須などの複雑な規則は課さない
 * （覚えられない規則は使い回しを招くため。OWASP の推奨に沿う）。
 */

export const PASSWORD_MIN_LENGTH = 10
export const PASSWORD_MAX_LENGTH = 128
